#!/usr/bin/env node

'use strict';

var path = require('path');

// set production environment
process.env.NODE_ENV = 'production';

process.bin = process.title = 'onestack';

// set workdir as server dir
process.chdir(path.join(__dirname, '../'));

// run grunt tests



