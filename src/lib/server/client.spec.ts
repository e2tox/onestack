// import { client, ServiceClient } from './client';
//
// @client('sw.engine.storage.StorageEngine')
// class TestAgentClass extends ServiceClient {
//
// }
//
// describe('client', () => {
//
//   describe('# should able to', () => {
//
//     it('new instance', () => {
//       const agent = new TestAgentClass('localhost:3000');
//       expect(Reflect.getPrototypeOf(agent)).toBe(TestAgentClass.prototype);
//       expect(agent instanceof TestAgentClass).toBe(true);
//     });
//
//   });
//
// });
