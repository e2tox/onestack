#!/usr/bin/env node

'use strict';

var path = require('path');

// set production environment
process.env.NODE_ENV = 'production';

process.bin = process.title = 'onestack - scaffold';

// set workdir as server dir
process.chdir(path.join(__dirname, '../'));

// boot the server
require('../server.js');



