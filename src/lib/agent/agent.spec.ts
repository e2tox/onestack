import { Agent } from './agent';

class Foo {
  bar():number {
    return 1;
  }
}

describe('Agent', () => {
  
  describe('# create', () => {
  
    it('initialize', () => {
      
      // const foo: Foo = new Agent(Foo);
      // console.log('create agent with domain')
    });
  
  });
  
});

